---
name: wt-wrapup
description: Wrap up a worktree once all changes are implemented and reviewed+approved
disable-model-invocation: true
---

Looks like all the changes we wanted our good.

Run a `git merge main` to pull the main branch (which may have diverged) into this branch. If there are any conflicts, resolve them. Assume the changes in the main branch are intentional. If you're unsure about a conflict, consult with me.
