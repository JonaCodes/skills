# Backend notes:

- Use a routes/controller/service setup.
- Each service should have its own directory, and within that directory you can feel free to break it down to meaningful sub directories.
- Use express for the server.
