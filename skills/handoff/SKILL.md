---
name: handoff
description: Do not use this skill unless the user explicitly asks.
manual-only: true
---

We've created a plan. Write a prompt I can feed, with relevant context, to a new agent to begin implementation based off this plan. The agent should only start with the first phase (if phases are part of this plan), but should have the full context so it knows what's going on.

This prompt should be meaningful but lean, and should mainly include what TO do. Avoid mentioning things that to NOT do.
